struct material
{
	float3	ambient;
	float3	diffuse;
	float3	specular;
	float	shininess;
};

uniform material	u_material;

float4 main(float2 vTexcoord : TEXCOORD0) : COLOR
{
	float3 m = (u_material.ambient + u_material.diffuse + u_material.specular);
	return float4(m, 1.0f);
}
